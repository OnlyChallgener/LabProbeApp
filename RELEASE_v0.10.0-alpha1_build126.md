# LabProbe v0.10.0-alpha1 build126

UI V2 第一版。该版本仅重构界面、导航、弹层、端口映射表单和功能入口图标。

保持不变：
- 设备图标和设备识别逻辑
- Hub API 路径和认证方式
- Ping / DNS / NAT / 漫游 / MTU / SSH 等测试逻辑
- 所有图表采样、坐标和事件逻辑

配套 Hub：v0.8.3。Rust LabRelay 继续使用 v0.1.0。
