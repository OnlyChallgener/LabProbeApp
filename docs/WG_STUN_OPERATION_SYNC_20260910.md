# WG / STUN 端口联动、操作反馈与通知修复

## 范围与基线

- 基于 `codex/ui-material-polish` / `be91c60`，工作分支 `codex/wg-stun-operation-sync`。
- 修复 Android WG、STUN、关联收藏的端口归属、增删改反馈及依赖安全；用户追加确认后，通知跳转、启动去重与 AI 通知内容一并纳入本轮。不调整原有字体、页面排版、隧道数据面或网络协议。
- 本机只执行 Python 检查；Android 编译、Kotlin 单元测试交给 GitHub Actions，不安装本地 SDK、模拟器。

## 实施方案

1. 网关 ListenPort 是当前受管 WG 服务的端口来源。新增客户端不更改服务器端口；自动 DDNS 默认跟随，手动配置保持独立。STUN Endpoint 使用实际公网映射，不等同于服务目标端口。
2. 修改网关时先读取当前修订和完整配置，保留 Peer、端点及未知字段，再提交；等待对应 Agent 应用回执，区分“已提交”“已应用”“穿透待就绪”。仅重定向明确引用该网关的 STUN 规则，不按名字或 51820 猜测。
3. 防火墙由现有 Agent 控制接口负责，不在 App 新增 SSH、绕过接口或清理用户规则。未收到明确回执时不宣称已生效；部分失败保留具体结果，不伪装成回滚成功。
4. WG / STUN 写操作共用应用生命周期的操作状态，点击立即反馈；密钥和存储工作离开主线程。阻止重复和冲突写入，关闭弹层不丢失进度，失败保留可恢复配置。
5. 严格按规则 ID 绑定：不自动换绑；删除 WG 不移除共用穿透或他人的 Endpoint；STUN 删除或改目标先检查引用。
6. 收藏仅同步来源地址及运行状态，保留用户编辑字段；用户取消收藏不在下次轮询重新出现。请求失败不当成空列表处理。
7. 通知点击按明确路由分发：设备上下线 → 设备页，其他网络事件及混合汇总 → 事件页，证书提醒 → 每日总结（包含证书区域），AI → 助手页的独立“AI 通知”内容框。不同通知使用独立身份，避免后来的通知覆盖先前通知的点击内容；热启动及旋转不重复消费。
8. 每次 App 会话首次同步静默建立通知基线，AI 分页历史持续静默吸收到追平；之后的事件批量合成一条提醒，AI 批量仅提醒最新一条。游标/已见事件先持久化，再发送通知；关闭权限也吸收已见记录，避免开启权限后补推旧消息。
9. AI API 没有通知对应的 conversationId，不猜测聊天会话、不把通知伪装成助手回复。独立详情显示通知原文，可选择/复制；刷新聊天历史不清除它。跨 Hub 通知标识仅用摘要，不把连接令牌写入 Intent 或通知地址。

## 验证计划

- Python：源代码约束、测试用例完整性、工作流和修改范围检查；明确这是静态回归检查，不替代 Kotlin 执行。
- GitHub：执行真实 Kotlin 单元测试与 Android 编译，保存测试报告及 APK。
- 必测：自定义 51826；独立手动端口；公网端口不同于目标；共享 STUN 删除；缺失绑定；失败/超时；连续点击；旧快照；收藏保留与取消收藏。
- 通知必测：冷/热启动路由；不同通知点击内容独立；历史分页静默；乱序/重复 ID；关闭权限后再启用不补推；事件批量一条；跨 Hub 隔离；AI 原文不进入聊天上下文。
- 真机/路由器验收：保存后即时反馈、页面仍可滚动；网关及防火墙回执；STUN 更新后新映射；实际 WG 握手。未实际执行的不写作 PASS。

## 实施与验证日志

- 2026-09-10：只读核对基线并新建分支。主任务负责端口/依赖核心；Sol 负责 WG 操作状态；Terra 负责 STUN/收藏；后续由轻量模型补充静态测试与复核。
- 2026-09-10：完成网关端口归属、旧 DDNS 自定义端口保留、STUN 精确 ID 绑定及共享规则删除保护；新增完整配置/修订回执检查，不再仅按保存请求成功宣布生效。
- 2026-09-10：WG / STUN 使用共享操作状态，写入时即时反馈；收藏保留用户元数据并记录取消收藏；网络请求期间发生 Hub 切换时停止后续提交。
- 2026-09-10：首轮 `python -m unittest discover -s tools -p 'test_*.py' -v`：15 项通过；`git diff --check` 通过。修改期间的后续验证以最终记录为准。
- 2026-09-10：独立复核后补齐启用网关的写锁、STUN 目标地址核对及基于新鲜网关状态的端口归属迁移。创建超时后不会因一次空列表自动允许重放；提供明确风险提示后的人工核对恢复入口。
- 2026-09-10：提交前再次执行 Python 静态回归：16 项通过；差异空白检查通过。新增 Kotlin 回归用例随代码提交，仅由 GitHub 执行。
- 2026-09-10：WG/STUN 首次推送 `9bec3c9`；[首轮 GitHub 构建](https://github.com/OnlyChallgener/LabProbeApp/actions/runs/34436951311) 的 Python 检查通过，Android 编译未通过，正在修正编译错误后重验。
- 2026-09-10：用户明确将通知加入本轮，Sol 处理 AI 通知原文/独立入口，Luna 处理事件/证书通知及静态约束，主任务处理启动基线、游标与路由，Terra 处理 GitHub 编译反馈。不在本机运行 Android 工具。
- 2026-09-10：修复首轮 CI 的 STUN `rememberUpdatedState` 缺失导入。通知追加后执行 Python 静态回归：24 项通过；差异空白检查通过。新增实际通知权限/去重、AI 游标迁移/顺序及通知载荷测试，仅提交 GitHub 执行。
- 通知追加后的 GitHub 结果：验证后补充。

## 明确限制与验收边界

- App 只使用已有 Hub / Agent API；没有改动或部署 Agent、防火墙实现。WG 生效取决于对应修订的 Agent 回执；防火墙细分规则仍须路由器实测，不能把单元测试写作真实放行验证。
- 网关和 STUN 分属不同接口，不宣称跨接口原子事务、零断连或已自动回滚。STUN 接口没有现成条件修订参数，App 做依赖校验、提交前重读和冲突提示，但无法保证另一台客户端恰在请求窗口内并发编辑时完全无竞争。
- 操作状态跨页面/弹层保留，非操作系统持久后台任务；强制结束 App 后需从服务器重新核对结果。未确认的创建不得凭等待超时直接视为失败并重复新增。
- 本机不执行 Android 构建、Kotlin 测试或真机网络测试；Python 是静态契约检查。远程编译结果与真实路由器/WG 握手验收分别记录。
- 通知使用“先记已见、再投递”的至多一次策略：进程恰在两步之间结束可能漏掉系统提醒，换取不重复轰炸；原始业务事件/AI 数据仍保留在服务端。AI 批量仅提示最新一条，不承诺每条都有系统提醒。通知不是可靠消息队列。
- 超长 AI 通知正文/标题在本地私有存储保存，Intent 与旋转保存状态仅携带引用，避免超大载荷；缓存限制为最近 100 个文本字段。超过保留范围的旧通知提示内容不可用，不展示其他通知原文。通知详情不等同于跳转某一聊天会话，因为当前服务端通知协议没有会话 ID。

## 官方依据

- [WireGuard Quick Start](https://www.wireguard.com/quickstart/)：ListenPort 与 Peer Endpoint 是不同参数。
- [RFC 8489 §2](https://www.rfc-editor.org/rfc/rfc8489.html#section-2)：STUN 返回经 NAT 映射的实际公网传输地址。
- [Android Coroutines Best Practices](https://developer.android.com/kotlin/coroutines/coroutines-best-practices)：耗时工作保证主线程安全，需跨页面完成的工作使用外部生命周期作用域，并暴露可观察状态。
- [Android Notification Navigation](https://developer.android.com/develop/ui/views/notifications/navigation)：通知入口保留合理的应用内导航与返回路径。
- [PendingIntent](https://developer.android.com/reference/android/app/PendingIntent)：Extras 不决定 PendingIntent 身份；通知使用明确且独立的 Intent 数据标识。
