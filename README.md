# LabProbe / 极客网探 v0.8.2 收敛修复版

本版基于 v0.8.1 做收敛修复，不改 Hub 与锐捷脚本：

- 去掉首页、参数卡、DNS 记录等区域的重叠色块，卡片改成单层 Surface。
- Ping 参数输入框回调到舒适高度，避免字缩在左下角或被裁切。
- Ping 图表按“统计小卡 + 清晰 X/Y 坐标 + 蓝色折线 + 淡蓝图面”重做，不再显示 AUTO。
- DNS 查询记录改成多行展示：时间/域名一行，A/AAAA 等结果独立横向滑动。
- 输入框、下拉框、按钮继续统一到 OneUI + Material 3 柔和圆角风格。
- 保留 v0.8.0/v0.8.1 的记录两行、每日备注、DNS 查询记录、返回键修复等功能。

上传到 GitHub 后运行 Actions → Android APK → Run workflow。
