# LabProbeApp v0.9.2

基于 v0.9.1 累计修复。

## 更新

1. 首页卡片标题从“VPN 地址”改为“VPN / STUN 地址”。
2. 支持显示 Hub 返回的 Lucky STUN 地址。
3. Lucky 地址原样显示，不拆 IP / 端口。
4. 没有 Lucky 数据时自动隐藏 Lucky 行。
5. 保留 v0.9.1 的 Ping 高频采样、自适应聚合曲线、峰值橙点、丢包红点优化。
