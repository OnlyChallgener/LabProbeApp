# APP build156 / Hub 0.9.24 deployment

Use the `build152` branch for APP 0.10.15 build156 and the `hub0919` branch for Hub 0.9.24.

This pairing restores the real router settings routes, uses Hub-native WSS for realtime delivery, keeps terminal sampling demand alive while the APP socket is connected, and carries BE72 2.4G/5G temperature plus storage fields.
