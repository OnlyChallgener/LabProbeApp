

# Android / Gradle
.gradle/
build/
app/build/
local.properties

# Signing keys - do not commit
signing.properties
*.jks
*.keystore
keystore/

# IDE
.idea/
*.iml
*.tar.gz

# Local Python UI guardrails
__pycache__/
*.py[cod]


*.apk

# Local reverse-engineering scratch (official APK, firmware tarballs, pcaps,
# analysis HTML). Large binaries, never shipped with the app.
# Anchored to the repo root on purpose: a bare `test/` would also swallow
# `app/src/test/`, which is real source.
/test/
