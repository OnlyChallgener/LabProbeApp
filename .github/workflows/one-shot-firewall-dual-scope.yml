name: One-shot firewall dual-scope test release

on:
  push:
    branches:
      - fix/firewall-dual-scope-01074
    paths:
      - .github/workflows/one-shot-firewall-dual-scope.yml

permissions:
  contents: write

jobs:
  patch-test-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/firewall-dual-scope-01074
          fetch-depth: 0

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: 9.5.1

      - name: Install Android SDK
        shell: bash
        run: |
          set -euo pipefail
          yes | "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" --licenses >/dev/null || true
          "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" "platforms;android-36" "build-tools;36.0.0" "platform-tools"

      - name: Apply focused patch
        shell: bash
        run: |
          set -euo pipefail
          python3 .github/scripts/firewall_dual_scope_patch.py
          grep -q 'versionCode = 229' app/build.gradle.kts
          grep -q 'versionName = "0.10.74"' app/build.gradle.kts
          grep -q '双栈规则同时参与本区域优先级' app/src/main/kotlin/com/labprobe/app/RouterControlUi.kt
          git diff --check

      - name: Prepare signing key
        env:
          LABPROBE_KEYSTORE_BASE64: ${{ secrets.LABPROBE_KEYSTORE_BASE64 }}
        shell: bash
        run: |
          set -euo pipefail
          printf '%s' "$LABPROBE_KEYSTORE_BASE64" | base64 -d > "$RUNNER_TEMP/labprobe-release.jks"
          echo "LABPROBE_KEYSTORE_PATH=$RUNNER_TEMP/labprobe-release.jks" >> "$GITHUB_ENV"

      - name: Test and build signed APK
        env:
          LABPROBE_KEYSTORE_PATH: ${{ env.LABPROBE_KEYSTORE_PATH }}
          LABPROBE_KEYSTORE_PASSWORD: ${{ secrets.LABPROBE_KEYSTORE_PASSWORD }}
          LABPROBE_KEY_ALIAS: ${{ secrets.LABPROBE_KEY_ALIAS }}
          LABPROBE_KEY_PASSWORD: ${{ secrets.LABPROBE_KEY_PASSWORD }}
        shell: bash
        run: |
          set -euo pipefail
          gradle :app:testDebugUnitTest :app:assembleRelease --stacktrace --console=plain
          APK=app/build/outputs/apk/release/app-release.apk
          test -s "$APK"
          unzip -t "$APK" >/dev/null
          "$ANDROID_HOME/build-tools/36.0.0/aapt" dump badging "$APK" | grep -q "versionCode='229' versionName='0.10.74'"
          "$ANDROID_HOME/build-tools/36.0.0/apksigner" verify --verbose --print-certs "$APK"
          sha256sum "$APK"

      - name: Commit clean source tree
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm -f .github/workflows/one-shot-firewall-dual-scope.yml .github/scripts/firewall_dual_scope_patch.py
          git add app/build.gradle.kts app/src/main/kotlin/com/labprobe/app/RouterControlUi.kt
          git commit -m "fix(app): model firewall priority per IPv4 and IPv6 scope"
          git push origin HEAD:fix/firewall-dual-scope-01074

      - name: Publish prerelease
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          TAG='test-bundle/20260902-15'
          APK=app/build/outputs/apk/release/app-release.apk
          ASSET="$RUNNER_TEMP/LabProbe-v0.10.74-build229-firewall-dual-scope-test.apk"
          cp "$APK" "$ASSET"
          cat > "$RUNNER_TEMP/notes.md" <<'EOF'
          测试范围：
          - 防火墙转发/入站/出站内按 IPv4、IPv6 两个真实优先级区域展示。
          - 双栈规则同时出现在 IPv4 与 IPv6 区，各自独立参与排序。
          - 排序忙碌期间拖动柄保留并置灰，不再整页消失。
          - 保留 0.10.73 已验证的 WireGuard 修复。
          EOF
          gh release create "$TAG" "$ASSET" --target "$(git rev-parse HEAD)" --title "极客网探 v0.10.74 build229 测试版 · 防火墙双栈排序" --notes-file "$RUNNER_TEMP/notes.md" --prerelease
