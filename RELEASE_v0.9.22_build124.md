# LabProbeApp v0.9.22 build124

仅新增端口映射工具：

- 工具页移除“服务监控”，替换为“端口映射”。
- 新图标与蓝色端口映射卡片纹理。
- 支持 TCP 6→4、6→6、6→6 IPv6 后缀匹配。
- 列表、筛选、新建/编辑、详情、启停、删除。
- 展示 Agent 状态、目标解析、当前连接数、上下行流量和近 1 小时图表。
- APP 不修改路由器防火墙。
